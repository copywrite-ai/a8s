- name: "{{ lookup('pipe', 'date +%H:%M:%S') }} | localhost | Serial: {{ item_plan.app_name }} | Start App"
  include_tasks: _deploy_start.yml

- name: "{{ lookup('pipe', 'date +%H:%M:%S') }} | localhost | Serial: {{ item_plan.app_name }} | Verify App"
  include_tasks: _deploy_verify.yml
